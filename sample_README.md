# sample_repo

`sample_repo` is a Python project containing 5 module(s). This documentation was generated by static analysis of the source (no code was executed).

## Architecture

The project is organised into 5 module(s) connected through their import relationships. Static analysis found 1 cyclic import dependency(ies), flagged below — these are the highest-risk coupling points in the codebase.

## ⚠️ Dependency Cycle Warnings

1 cyclic import dependency(ies) were detected:

### HIGH — `customer → order → customer`

These modules import each other, forming a cycle. Cyclic imports make initialization order fragile, can cause ImportError at runtime, and couple the modules so they cannot be understood or tested in isolation.

**Suggested fix:** Extract the shared types/functions into a third module that both depend on, defer one import to inside the function that needs it, or merge the modules if the split is artificial.

## Installation

```bash
git clone <repo-url>
cd sample_repo
pip install -e .
```

## Usage

Import the modules you need and call their functions/classes directly. See the per-module reference below.

## Module Reference

### `__init__.py`

A tiny demo store package used to exercise codescribe.

### `customer.py`

Customer domain model.

**Responsibilities:**

- Defines class `Customer`

#### Classes

- **`Customer`** — A store customer who can place orders.
  - Key methods: place_order

### `main.py`

Entry point that wires the demo store together.

**Responsibilities:**

- Provides function(s): `run_demo`

#### Functions

- **`def run_demo() -> str`** — Create a customer, place an order, and return its receipt.

### `order.py`

Order domain model.

**Responsibilities:**

- Defines class `Order`

#### Classes

- **`Order`** — An order placed by a customer for a list of items.
  - Key methods: total, receipt

### `utils.py`

Stateless helper functions with no internal dependencies (a clean leaf).

**Responsibilities:**

- Provides function(s): `format_currency`, `slugify`

#### Functions

- **`def format_currency(amount: float, symbol: str = '$') -> str`** — Format a numeric amount as a currency string with two decimals.
- **`def slugify(text: str) -> str`** — Convert arbitrary text into a lowercase, hyphenated slug.

---

_Generated by [codescribe](https://github.com/) — deterministic AST extraction → dependency-graph analysis → schema-validated LLM documentation._
